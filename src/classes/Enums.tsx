export enum EventChannel{
    INTERNAL = "internal",
    EXTERNAL = "external",
    GLOBAL = "global",
    UNDEFINED = "undefined"
}

export enum MemoryMode {
    DISTRIBUTED = "distributed",
    SHARED = "shared",
    UNDEFINED = "undefined"
}

export enum Version {
    ZERO_ONE = "0.1",
    UNDEFINED = "undefined"
}