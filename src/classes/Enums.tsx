export enum EventChannel{
    INTERNAL = "internal",
    EXTERNAL = "external",
    GLOBAL = "global",
    UNDEFINED = "undefined"
}

export enum MemoryMode {
    DISTRIBUTED = "distributed",
    SHARED = "shared"
}