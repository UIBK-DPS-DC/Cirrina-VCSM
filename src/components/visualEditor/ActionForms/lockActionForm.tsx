import Action from "../../../classes/action.tsx";

export default function LockActionForm(_props: {action: Action | undefined}) {
    return(
        <div className={"action-form-container"}>
            <h2>This is a Lock Action</h2>
        </div>
    )
}